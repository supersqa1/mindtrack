# how to revoke and geneate new key for user
# what are the limitations on the api, like any throtling, allow only to do things the user created?

# what is the point of this api?
# Get your API key
curl http://localhost:6000/me/api-key \
  -H "X-API-Key: YOUR_API_KEY"



# todo:
Generate API doc

# the create task endpoint, it just creaes tasks, should we have who created it?
# should the creae task use a dummy key instead of blank?


# what is the point of
@user_bp.route('/me/api-key', methods=['GET'])
@require_api_key
def get_api_key():
    api_key = request.headers.get('X-API-Key')
    return jsonify({"api_key": api_key})

# only toggleing statuses and not haveing multiple statuses

# we build auth from scratch, were we supposed to use some known library?